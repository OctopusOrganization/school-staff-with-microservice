### Spring properties
# Service registers under this name
spring:
  application:
    name: Movies-Service

# Discovery Server Access
eureka:
    client:
      serviceUrl:
        defaultZone: http://localhost:1111/eureka/